<!doctype html>
html(lang="en")
    head
        meta(charset="utf-8")
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta.3/css/bootstrap.min.css" integrity="sha384-Zug+QiDoJOrZ5t4lssLdxGhVrurbmBWopoEl+M6BdEfwnCJZtKxi1KgxUyJq13dy" crossorigin="anonymous">

        title="Hello, world!"
    body
        .container
            .row.mt-5
                .col-12
                    form.form-inline(action="/uploadImage", method="post", enctype="multipart/form-data")
                        .form-group
                            input.form-control-file(type="file", name="image", accept="image/*")
                        input.btn.btn-success(type="submit", value="Upload!")

            .row.mt-5
                //- Print out all of the images
                each image in images
                    .col-sm-6.col-md-4
                        .card.mb-3
                            img.card-img-top(src="/images/" + image)
                            .card-body
                                a.btn.btn-outline-danger.float-right(href="/deleteImage/" + image)="🗑"
                else
                    .col-12.text-center
                        h1.display-1 😢
                        p="No images yet!"